{
  "name": "agentgate-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-assets": "node scripts/sync-assets.mjs",
    "dev": "npm run sync-assets && wrangler dev",
    "deploy": "npm run sync-assets && wrangler deploy",
    "build": "npm run sync-assets",
    "typecheck": "tsc --noEmit",
    "test": "npm run sync-assets && vitest run"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "jose": "~6.2.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.40",
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "^3.99.0"
  }
}
